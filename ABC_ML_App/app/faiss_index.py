"""FAISS index utilities (placeholder).

Will implement vector extraction and FAISS indexing in a later step.
"""

def build_index(vectors):
    # Placeholder returning a dict for now; will integrate faiss later.
    return {"n_vectors": len(vectors)}
